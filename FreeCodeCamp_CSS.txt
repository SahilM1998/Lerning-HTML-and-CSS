Link tag in HTM head tag is used to link the css file to HTML it is called as external styling

Selectors

Selectors are way of grabbing and manipulating HTML

element selector - select entire element
Class selector - select element with certain class name
can be used multiple times
.className
ID slector  - select element with certain ID name
can be used on one element at a time 
#IDName

Specificity and when to use
Talks about rewritting of properties in css

element<Class<ID<Inline styling
class are user multiple type
Id supposed to be used only once

pseudo selector
selector:hover{}

firstchild
lastchild
nth-child(n)
only-child






